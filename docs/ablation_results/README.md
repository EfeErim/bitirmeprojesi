# ROI Ablation Results

Notebook 10-14 write their reports under this folder so Colab runs produce repo-visible artifacts instead of ignored local `outputs/` files.

Expected subfolders:

- `full_image_baseline/`
- `primary_roi_inference/`
- `hybrid_roi_fallback/`
- `roi_trained_adapter/`
- `mixed_full_roi_training/`

Each completed inference ablation writes `report.json` and `rows.csv`.
