interface:
  display_name: "AADS Inference Runtime"
  short_description: "AADS v6 inference, calibrated router handoff, adapter loading, and runtime guidance"
  default_prompt: "Use $aads-inference-runtime to fix router inference, calibrated handoff behavior, payload contracts, or adapter runtime behavior in this repo."
