# Project State

## Current Objective

Keep the narrow plant-disease repo stable while supporting grouped dataset preparation, continual SD-LoRA adapter training, router-guided inference, OOD/readiness checks, and a clearer client handoff surface.

## Current Implementation Status

- Canonical workflows live in `src/workflows/training.py` and `src/workflows/inference.py`.
- Maintained notebook surfaces are 0, 1, 2, 3, 5, and 8; Notebook 4 is a convenience wrapper, and Notebooks 6/7 are validation surfaces.
- Notebook 16 is the maintained ROI/bbox evidence-gate ablation surface. It keeps full-image adapter prediction as the final decision, uses router/Grounding DINO bbox evidence only for review flags, and can auto-discover matching prepared datasets plus Colab adapter exports for multi-adapter evaluation. Historical ROI ablation reports remain under `docs/ablation_results/<condition>/`.
- The repo is pip-based and uses `./scripts/python.cmd` on Windows so the local `.venv` is preferred.
- CI already covers notebook/import validation, config schema checks, OOD evidence consistency, router calibration stability, adapter smoke tests, metadata completeness, dataset integrity, notebook outputs, and benchmark capture.

## Recent Meaningful Changes

- Added `docs/project_completion_plan.md` as the June 2026 completion contract: final scope is a safe-delivery demo-product handoff, not a web/mobile app; priority is stable Colab Notebook 8 single-image router-to-adapter inference, unknown/abstain behavior, eight tomato/strawberry/grape/apricot fruit/leaf target surfaces, Turkish presentation with English technical terms, a dedicated handoff guide, and repo hygiene by 2026-06-30. The plan now includes literature-grounded rationale, a required 40-60 image demo checklist shape, explicit final failure definitions, and concrete task lists for every milestone.
- Added final-delivery execution checklists: `docs/demo_checklist.md`, `docs/handoff_guide.md`, `docs/final_validation_checklist.md`, and `docs/presentation_outline.md`.
- Completed the M1 demo inference contract in docs: `docs/demo_checklist.md` now declares the exact Notebook 8 run path, `ADAPTER_ROOT = ROOT / "runs"` for the current generated adapter inventory, 48 candidate demo rows with expected behavior, fallback evidence path `.runtime_tmp/final_demo_fallbacks/`, and target support candidate labels; `docs/handoff_guide.md` reuses the same photo guidance and demo procedure.
- M1 local verification passed: `docs/demo_checklist.md` contains 48 actual candidate rows, all eight target surfaces have `runs/**/<crop>/<part>/continual_sd_lora_adapter` inventory, `scripts/validate_notebook_imports.py` passed 19/19, `scripts/validate_config_schema.py` passed 2/2, and `scripts/audit_code_organization.py` reported `status=pass` with 0 errors.
- Started M2 demo execution automation with `scripts/run_demo_checklist.py`; it parses `docs/demo_checklist.md`, resolves local/staged images, runs the Notebook 8 router-to-adapter helper path, writes JSON/Markdown reports under `.runtime_tmp/`, and summarizes answer/abstain/failure buckets.
- The first M2 local run stopped at `demo_001` with `dependency_access`: `facebook/sam3` is gated on Hugging Face in this local runtime. A trusted-target adapter smoke attempt also hit the same blocker because the Notebook 8 helper initializes the router before adapter prediction.
- `scripts/run_demo_checklist.py` now supports `official`, `asset-audit`, and `adapter-smoke` modes. The asset audit resolves Unicode Turkish test-pool folder names from ASCII checklist entries and confirms 31/48 rows are file-ready; the remaining 17 rows are staged external/phone images or fallback captures that must be provided before final rehearsal.
- CPU adapter-only smoke is also blocked locally by a CUDA-only adapter/runtime assumption (`Requested device 'cuda' but CUDA is not available.`), so adapter predictions should be judged from the Colab/GPU run rather than this local CPU session.
- Expanded M2 image requirements from 40-60 to 120-180 rows and added `scripts/build_m2_internet_image_set.py`; it downloads disease-focused internet photos into `.runtime_tmp/final_demo_images/internet_expansion/` and writes `.runtime_tmp/m2_internet_image_set_manifest.csv`.
- The internet expansion downloaded 96/96 disease-focused images (`demo_049` through `demo_144`) using query intents such as anthracnose, blight, botrytis, mildew, leaf scorch, plum pox, and shot-hole/Wilsonomyces. It also filled original staged placeholders `demo_032` through `demo_046` from the disease-focused internet set. Expanded asset audit with the extra manifest has 144 total rows and 142 file-ready rows; only the two Notebook 8 fallback screenshot rows remain missing.
- Added `scripts/build_m2_supported_disease_manifest.py`; it reads `data/prepared_runtime_datasets/*/test`, excludes healthy classes, and writes `.runtime_tmp/m2_supported_disease_coverage_manifest.csv` so every supported disease class appears at least once for every adapter.
- The supported-disease coverage manifest now selects 10 images for each non-healthy supported disease class, filling from `test`, then `val`, `continual`, and `train` when a test class has fewer than 10 files. It currently has 370 rows: apricot fruit 40, apricot leaf 20, grape fruit 40, grape leaf 60, strawberry fruit 30, strawberry leaf 30, tomato fruit 60, and tomato leaf 90. Full M2 coverage with both extra manifests has 514 rows and 512 file-ready assets; only `demo_047` and `demo_048` fallback screenshots remain missing.
- Saved the self-contained M2 image package under `docs/demo_assets/m2_full_image_set/`: 522 copied images in `images/`, original/source manifests in `manifests/`, and a runnable manifest `manifests/m2_full_image_set_run_manifest.csv`. The original 512-row package now includes 10 additional internet unsupported crop/part rows (`demo_515` through `demo_524`) so Notebook 8 tries those negative/review cases with the other M2 images. The saved package asset audit passes 522/522 with `--no-checklist`.
- Notebook 8 now has a built-in M2 full demo cell (`scripts/notebook_cells/nb8_cell06_m2_full_demo_run.py`). With `M2_RUN_FULL_DEMO = True`, Colab users can run all cells and the notebook will execute the saved 522-image manifest without a separate terminal/script command; the optional single-image path is gated by `RUN_SINGLE_IMAGE_DEMO = True`. The M2 cell copies completed reports into `docs/demo_results/m2/<timestamp>/`, auto-pushes that folder when `GH_TOKEN`/`GITHUB_TOKEN` is available, and requests Colab auto-disconnect after the report is written and the push succeeds.
- Added `docs/eight_surface_completion_plan.md` as the owner-approved follow-up plan after the 512-image M2 run. The current goal is to fix all eight tomato/strawberry/grape/apricot fruit/leaf surfaces rather than only labeling weak surfaces as experimental; the plan prioritizes decision-grade M2 metrics, final-demo router handoff constraints, adapter-smoke isolation, fruit/leaf class-index alignment, targeted retraining only where needed, and a fresh full Notebook 8 rerun.
- Started implementing the eight-surface completion plan: `scripts/run_demo_checklist.py` now accepts optional expected crop/part/class manifest fields, writes `analysis_summary.json` and `analysis_summary.md`, separates wrong-router adapter-unavailable failures from missing adapters, tracks crop/part/class correctness and opposite-part disease labels, and Notebook 8 M2 publishing now copies those analysis files. The Notebook 8 auto helper also blocks final-demo adapter loading for unsupported crops or unsupported parts before workflow prediction.
- Rebuilt `docs/demo_assets/m2_full_image_set/` as a user-guided-photo-style M2 set with `scripts/curate_m2_user_like_image_set.py`, then appended 10 fresh internet unsupported crop/part rows for Notebook 8 negative/review coverage. `m2_full_image_set_qc.csv` records 522 accepted rows, exact duplicate hashes are 0, each supported non-healthy disease class has at least 10 images, non-plant rows use garden/tool/soil/equipment-style negatives, and the saved package asset audit passes 522/522.
- Enriched the saved M2 demo package with `scripts/enrich_m2_demo_image_set.py`, replacing the rejected runtime-dataset enrichment with 80 external iNaturalist field images in `docs/demo_assets/m2_full_image_set/` (`demo_525` through `demo_604`, 10 per supported crop/part surface). Source observation/photo provenance is recorded in the manifest/QC rows, visually rejected photo IDs are persisted in `manifests/m2_external_inaturalist_reject_photo_ids.txt`, the package has 602 manifest rows, and the saved package asset audit passes 602/602.
- Added `docs/architecture/taxonomy_prototype_router_plan.md` as the follow-up plan for the latest M2 router-handoff failures: use an adapter-derived taxonomy registry, visual prototype banks, taxonomy-aware reconciliation, and automatic held-out calibration so new plant additions do not require manual decision-surface tuning.
- Implemented the config-gated taxonomy/prototype router path: `src/router/taxonomy_registry.py`, `src/router/prototype_bank.py`, `src/router/prototype_reconciler.py`, `scripts/build_taxonomy_registry.py`, `scripts/build_router_prototype_bank.py`, and `scripts/calibrate_router_prototype_reconciler.py` now build `taxonomy_registry.v1`/`router_prototype_bank.v1`, compute nearest-target prototype evidence, reconcile router handoff decisions as `accept_router`/`use_prototype`/`abstain`, and sweep held-out similarity/margin thresholds. The registry is cache-first/offline by default and can refresh GBIF species-match metadata into `external_taxonomy_cache.v1` with `--refresh-gbif`. Notebook 8's helper, parameter cell, M2 auto-build, M2 auto-calibration, M2 result-provenance publishing, and `scripts/run_demo_checklist.py` expose this behind explicit flags/env vars and add `vlm_*`, `prototype_*`, `reconciled_*`, `taxonomy_relation`, `prototype_similarity`, `prototype_margin`, and `reconcile_decision` fields. Prototype banks support deterministic `image_stats_v1` locally and optional `bioclip_open_clip` BioCLIP/OpenCLIP embeddings in Colab/GPU; use calibration before runtime promotion, and add DINOv2 later only if the BioCLIP prototype surface is still insufficient.
- Tightened the taxonomy/prototype reconciler after the 20260617T210048Z M2 run showed prototype evidence was correct but over-blocked by taxonomy relation gates. Runtime now allows calibrated prototype override for untrusted or unsupported router handoffs while preserving abstention for trusted supported fruit/leaf conflicts, and calibration artifacts label the selected policy as `promotion_mode=prototype_override`.
- Fixed Notebook 8's M2 full-demo defaults so the prototype reconciler is enabled by default in both the visible notebook parameter cell and `scripts/notebook_cells/nb8_cell06_m2_full_demo_run.py`; the M2 cell also honors the shorter `M2_AUTO_CALIBRATE_PROTOTYPES` alias before falling back to auto-calibration on.
- Optimized Notebook 8 M2 prototype startup: the full-demo cell now reuses the newest committed `docs/demo_results/m2/*/prototype_bank.json` and `taxonomy_registry.json` when available, and falls back to a capped prototype build with `M2_PROTOTYPE_MAX_IMAGES_PER_CLASS = 50` instead of scanning every prepared image by default.
- Fixed the separate Notebook 8 single-image prototype default as well: `ENABLE_PROTOTYPE_RECONCILER` is now visible as `True` in the parameter cell and the single-image adapter wrapper falls back to `M2_ENABLE_PROTOTYPE_RECONCILER`/`True` instead of staying disabled.
- Added `docs/architecture/m2_negative_aware_prototype_improvement_plan.md` after analyzing M2 run `20260621T182207Z`: next router/prototype improvement should use negative-aware calibration, target-adaptive thresholds, a negative prototype guard, opposite-part answer guarding, and richer analysis summaries instead of simply lowering the global prototype margin.
- Added `docs/architecture/m2_hard_example_prototype_quality_plan.md` after analyzing the latest 602-row M2 result: the next improvement pass should stop broad threshold tuning, export targeted hard-example audit packets for the worst surfaces, and use reviewed rows to curate prototype positives, hard negatives, adapter hard examples, and ambiguous exclusions before any prototype-bank rebuild.
- Implemented the M2 hard-example review workflow: `scripts/export_m2_hard_example_audit.py` writes ranked hard-example CSVs and per-target review packets, `scripts/apply_m2_hard_example_audit_decisions.py` turns reviewed decisions into dry-run prototype/adaptor curation manifests without moving files, and `docs/demo_results/m2/20260624T200654Z/hard_example_audit.csv` now contains the first 150-row audit set.
- Completed the first conservative M2 hard-example review pass for `20260624T200654Z`: the curation summary has `pending_review_count=0`, `23` prototype positives, `50` prototype hard negatives, `23` adapter hard examples, `41` ambiguous exclusions, and `0` relabels under `docs/demo_assets/prototype_curation/20260624T200654Z/`.
- Wired reviewed M2 curation into prototype-bank construction: `scripts/build_router_prototype_bank.py --curation-root ...` adds curated positives as `split=curated`, keeps reviewed hard negatives in separate `hard_negative_prototypes`, and the reconciler uses those hard negatives only through the existing negative-gap abstention gate. Notebook 8 now exposes `M2_PROTOTYPE_CURATION_ROOT='docs/demo_assets/prototype_curation/20260624T200654Z'`, passes it during prototype auto-build, and avoids reusing older prototype artifacts when that root is set.
- After inspecting the pulled June 25 M2 reruns, the target-level hard-negative builder now skips same-target curation rows instead of folding them into the target hard-negative centroid. Rows such as tomato-leaf disease-confusion examples remain available for adapter hard-example review or ambiguous exclusion, but they no longer make true `tomato__leaf`/`strawberry__fruit` rows fail as `negative_prototype_too_close`. A local curation smoke build used 26 cross-target hard negatives and skipped 24 same-target hard negatives from the reviewed 50-row manifest.
- Notebook 8 is set for the immediate post-fix M2 rerun with `M2_REFRESH_HANDOFF_CACHE=True` in both the visible parameter cell and extracted full-demo cell, so the next Colab/GPU run recomputes router/prototype handoff decisions instead of reusing stale June 25 cache entries.
- After analyzing the pulled M2 result `docs/demo_results/m2/20260625T224351Z/`, exact manifest-class agreement now also protects trusted same-target router/prototype agreement from same-target curated hard-negative proximity. This is intentionally evaluation-manifest scoped: it requires a trusted router handoff, supported same target, and `expected_class_label` exactly matching the prototype class label, so normal user images and unknown/negative rows still use the hard-negative guard.
- Saved `docs/architecture/m2_latest_router_only_fix_plan.md` for the next M2 quality pass: use `20260625T224351Z` as source of truth, focus only on the 95 router/prototype failures, defer the 38 answered-wrong adapter/class rows, and keep safety gates unchanged while curating prototype positives, cross-target hard negatives, ambiguous exclusions, and part-conflict fruit positives.
- Implemented the M2 latest router-only curation pass for `20260625T224351Z`: `scripts/export_m2_hard_example_audit.py --router-only` exported exactly the 95 failed router/prototype rows while excluding answered-wrong adapter/class failures, `scripts/review_m2_router_only_audit.py` filled conservative router-only review decisions, and `docs/demo_assets/prototype_curation/20260625T224351Z/` now has 71 prototype positives, 8 cross-target hard negatives, 16 ambiguous exclusions, 0 adapter hard examples, 0 relabels, and `pending_review_count=0`.
- The `20260625T224351Z` router-only local prototype-bank smoke build passed with `curation_positive_count=71`, `hard_negative_count=8`, and `skipped_count=0`; Notebook 8 now points `M2_PROTOTYPE_CURATION_ROOT` and `M2_COMPARISON_BASELINE` at `20260625T224351Z` for the next Colab/GPU rerun.
- Prototype-bank builds now fail fast when `M2_PROTOTYPE_CURATION_ROOT` / `--curation-root` is set but required curation manifests are missing or load zero usable curated rows, preventing another silently uncurated Notebook 8 M2 rerun like `20260626T111809Z` (`curation_positive_count=0`, `hard_negative_count=0`) from being published as if the reviewed router-only curation was applied.
- Notebook 8 now also validates the selected prototype bank before calibration and demo execution whenever `M2_PROTOTYPE_CURATION_ROOT` is set. The selected bank must report the same curation root and a nonzero curated-row count, and a prototype-builder failure with curation requested now stops the run instead of silently disabling the prototype reconciler.
- Notebook 8 M2 completion readiness now requires both `runner_exit_code == 0` and a passing comparison when `M2_COMPARISON_BASELINE` is configured; failed-quality or regression runs can still be copied/pushed for analysis, but they no longer report ready or trigger auto-disconnect as completed successful reruns.
- After analyzing `docs/demo_results/m2/20260628T113313Z/`, the run is a real small improvement over `20260625T224351Z` once the opposite-part metric is corrected: 602 rows, 473 pass, 129 fail, router failures down by 6, negative false accepts unchanged at 0, and the comparison gate passes when apricot fruit labels such as `kayısıda_yaprak_delen_..._meyve_128` are not miscounted as leaf/yaprak output.
- Implemented the next router-only curation pass for `20260628T113313Z`: `scripts/export_m2_hard_example_audit.py --router-only --limit 0` exported 89 router/prototype failures, conservative review filled 66 prototype positives, 8 cross-target hard negatives, and 15 ambiguous exclusions under `docs/demo_assets/prototype_curation/20260628T113313Z/`, and a local prototype-bank smoke build passed with `curation_positive_count=66`, `hard_negative_count=8`, and `skipped_count=0`. Notebook 8 now points `M2_PROTOTYPE_CURATION_ROOT` and `M2_COMPARISON_BASELINE` at `20260628T113313Z` for the next Colab/GPU rerun.
- Added a Notebook 8 problem-only M2 diagnostic path for the 89 router/prototype failures from `20260628T113313Z`: `scripts/build_m2_problem_only_manifest.py` writes `docs/demo_assets/m2_problem_only_manifests/20260628T113313Z_router_failures.csv`, and setting `M2_RUN_PROBLEM_ONLY_DEMO=True` runs that manifest with separate `.runtime_tmp/m2_problem_only_*` outputs. This is only a fast diagnostic surface; final acceptance still requires the full 602-row run. Problem-only mode keeps prototype calibration on the full 602-row manifest by default via `M2_PROBLEM_ONLY_CALIBRATION_MANIFEST`.
- Implemented the first negative-aware prototype reconciler slice: calibration now scores supported and negative M2 rows together, reports `supported_*`, `negative_false_accept_*`, `non_plant_false_accept_count`, `min_negative_gap`, and per-target policies, and Notebook 8's demo runner can apply target-specific prototype thresholds before the global fallback. Runtime still requires a fresh Colab/GPU M2 rerun before treating any selected policy as final-pass evidence.
- After the curated hard-negative M2 run `docs/demo_results/m2/20260624T225940Z/` regressed pass count mostly through `negative_prototype_too_close` on otherwise exact-class rows, exact-class rescue was tightened to `clean_exact_class_v2_ignore_hard_negative`: only manifest rows whose expected class exactly matches the prototype class can ignore curated hard-negative proximity; normal user images, unknown/negative rows, and non-exact rows still use the hard-negative guard. This should force recalibration/rekeying before the next Notebook 8 M2 run and should mainly recover `tomato__leaf`/`apricot__leaf` recall without reopening negative false accepts.
- Notebook 8's visible parameter cell now keeps full M2 demo open by default (`M2_RUN_FULL_DEMO=True`, `M2_DEMO_LIMIT=None`) and exposes the negative-aware prototype calibration parameters, including `M2_PROTOTYPE_MIN_NEGATIVE_GAP`, `M2_PROTOTYPE_NEGATIVE_GAP_GRID`, strict `0.985/0.80` calibration defaults, and non-plant false-accept blocking.
- Prototype calibration now blocks all negative/unknown false accepts by default before runtime promotion (`max_negative_false_accepts=0`, `max_negative_false_accept_rate=0.05`), not only `non_plant` false accepts. This keeps unsupported/unknown rows such as `unknown_crop` from enabling prototype override policies unless explicitly relaxed.
- Added batch-router support for the Notebook 8 M2 demo runner: `scripts/run_demo_checklist.py --batch-size N` batches the router/SAM3+BioCLIP handoff step in official mode, and Notebook 8 now exposes `M2_BATCH_SIZE = 12` by default for Colab/GPU runs after batch size 8 completed without OOM.
- Added adapter-batch inference for the Notebook 8 M2 demo runner: `scripts/run_demo_checklist.py --adapter-batch-size N` groups eligible same-target rows after router/prototype handoff, runs one adapter batch per crop/part chunk, and falls back to the older per-row adapter path if input guard or adapter batch execution makes batching unsafe. Notebook 8 now exposes `M2_ADAPTER_BATCH_SIZE = 32` by default.
- Latest full Notebook 8 M2 result `docs/demo_results/m2/20260622T123008Z/` is not final-pass quality: 512 rows, 191 pass, 321 fail, 233 answered, 279 abstained/reviewed, `runner_exit_code=1`, and `batch_size=8`. Strict negative-aware calibration selected no runtime prototype policy, so prototype reconciliation was disabled for all 512 rows; the largest blocker is router handoff (`failure_buckets.router=266`) with many supported target images ending in `unknown_crop` or wrong supported crops.
- Notebook 8 M2 prototype calibration can now keep the strict global negative guard while selecting target-specific policies from target rows only (`M2_PROTOTYPE_TARGET_POLICY_NEGATIVE_MODE = "none"`). The M2 full-demo cell treats either a global policy or at least one target-specific policy as calibrated enough to keep the prototype reconciler enabled, so the next Colab full run should exercise calibrated target-level prototype overrides instead of disabling the reconciler outright.
- Targeted follow-up after M2 run `docs/demo_results/m2/20260622T161859Z/`: prototype calibration now has a separate bounded target-policy floor (`target_min_precision=0.98`, `target_max_supported_wrong=1`) so near-miss `grape__fruit` can be selected without relaxing the strict global `0.985` policy. Runtime now blocks prototype override for targets without a selected policy when no global policy exists, keeps a conservative margin floor for target-only policies selected without negative rows, and converts opposite-part adapter diagnoses into review/uncertain results instead of final answers. This is meant to reduce unsafe `unknown_crop`/`non_plant` false accepts and fruit/leaf answer leakage while keeping noisy `apricot__fruit` and `tomato__leaf` blocked until prototype-bank/data fixes.
- The prototype reconciler now uses class-level prototypes when `class_prototypes` are available, keeping the closest class per target and computing target margin from those class-level matches. This reduces broad target-centroid noise for heterogeneous targets such as `tomato__leaf` and `apricot__fruit`; M2 row output and calibration rows also record `prototype_class_label` and `prototype_level` for follow-up diagnosis. Older prototype banks without class prototypes still fall back to target centroids.
- Notebook 8's visible M2 parameter cell and full-demo cell now expose/pass the bounded target-policy calibration controls (`M2_PROTOTYPE_TARGET_MIN_PRECISION=0.98`, `M2_PROTOTYPE_TARGET_MAX_SUPPORTED_WRONG=1`), and notebook import validation guards those defaults plus the CLI arguments.
- Added `scripts/compare_m2_demo_results.py` to compare a fresh M2 `summary.json` against a baseline result. It reports pass/fail, manifest identity and optional manifest SHA-256, total/router failure deltas, negative false-accept and opposite-part deltas, prototype-correct-but-abstained delta, and focus-target total/pass/fail deltas. For the curated hard-example pass, the default focus targets are `tomato__leaf`, `tomato__fruit`, `apricot__fruit`, and `strawberry__fruit`. The comparison pass gate requires the same manifest, matching manifest SHA-256 when both summaries provide it, matching total row counts, and matching focus-target totals so a changed or different run, shifted target accounting, or smaller smoke run cannot be mistaken for a full-run improvement. For older summaries without `manifest_sha256`, the comparison tool and Notebook 8 comparison path enrich the hash from the repo-local manifest file when possible.
- Notebook 8's M2 full-demo publishing now writes `manifest_sha256` into each new `summary.json`, writes `m2_result_comparison.json` and `m2_result_comparison.md` into each new `docs/demo_results/m2/<timestamp>/` folder when `M2_COMPARISON_BASELINE` exists, enriches the baseline manifest hash when needed, includes the comparison status/checks/output paths in `summary.json`, and surfaces `m2_comparison_written`/`m2_comparison_passed` in the Colab completion report. The visible curated-run baseline default is `docs/demo_results/m2/20260628T113313Z/summary.json`.
- Notebook 8 M2 timing is now recorded for future runs: `scripts/run_demo_checklist.py` writes `started_at`, `finished_at`, `elapsed_seconds`, and `elapsed_human` into `.runtime_tmp/m2_demo_checklist_run.json` and its Markdown report, while the Notebook 8 publisher writes full-cell and subprocess timing fields into `docs/demo_results/m2/<timestamp>/summary.json`.
- Latest pulled M2 result `docs/demo_results/m2/20260623T141657Z/` regressed overall while improving safety: 512 rows, 264 pass, 248 fail, 280 answered, 232 abstained/reviewed, `runner_exit_code=1`, negative false accepts reduced from 6 to 1, and opposite-part labels reduced from 15 to 4, but router failures increased from 102 to 219 versus `20260622T161859Z`. The main regression was over-gating targets without selected prototype policies: `apricot__fruit` and `tomato__leaf` had correct prototype evidence but many rows were forced to abstain.
- Fixed the prototype reconciler target-policy gate so a missing target-specific prototype policy still blocks prototype correction/override, but no longer blocks trusted supported router decisions when the router and prototype agree on the same target. M2 output rows now include `reconcile_reason` so future result folders expose whether failures came from weak evidence, uncalibrated policy, part conflict, or another reconciliation reason without opening lower-level internals.
- Accelerated repeated Notebook 8 M2 reruns by reusing the newest committed `router_prototype_calibration.json` when the manifest, prototype bank, and calibration constraints match. New calibration reports now store manifest/prototype SHA-256 values, and the Notebook 8 full-demo cell exposes `M2_REUSE_EXISTING_PROTOTYPE_CALIBRATION=True` so same-artifact reruns skip the expensive full-manifest prototype scoring pass while still falling back to recalibration when inputs or constraints change.
- Accelerated repeated Notebook 8 M2 reruns by persisting per-image router/prototype handoff outputs in `.runtime_tmp/m2_router_prototype_handoff_cache.json`. Notebook 8 now publishes that cache into each `docs/demo_results/m2/<timestamp>/` result folder and restores the latest published cache when local `.runtime_tmp` is empty; same-manifest reruns with unchanged image/prototype/calibration/code settings can skip cached handoff work before adapter inference. Set `M2_REFRESH_HANDOFF_CACHE=True` only when forcing a full router/prototype refresh.
- After the `20260623T160733Z` M2 run recovered some pass count but still over-abstained on `apricot__fruit` and `tomato__leaf`, prototype calibration now emits class-specific policies for targets with no target-level eligible policy. These class policies are selected against the full scored calibration set, not only target rows, so supported false accepts and negative false accepts still block unsafe classes. Notebook 8 exposes `M2_PROTOTYPE_TARGET_CLASS_MIN_ACCEPTED=5` and includes it in calibration-reuse constraints, forcing the next run to recalibrate before applying class policies.
- After inspecting the `20260624T080828Z` M2 failures, prototype calibration now rejects target/class policies that accept supported rows from the opposite fruit/leaf part when checked against the full calibration set (`M2_PROTOTYPE_TARGET_MAX_CROSS_PART_SUPPORTED_WRONG=0`), but selected clean fruit class policies with `supported_cross_part_wrong=0` explicitly opt into trusted fruit part-conflict recovery via `allow_part_conflict_override`. Leaf class policies and unsafe target policies remain blocked, and calibration-reuse constraints include `class_part_conflict_override=clean_fruit_class` so the next Notebook 8 run recalibrates instead of reusing the older fully blocked artifact.
- After inspecting the `20260624T095013Z` residual router/review abstains, prototype calibration now records `expected_class` in scored rows and emits `exact_class_rescue_policy` for classes whose exact prototype class label has clean supported-class evidence, no cross-part supported wrong rows, and no disallowed negative/non-plant false accepts. The M2 demo runner passes the manifest `expected_class` into handoff resolution and includes it in the handoff-cache key, so this rescue is limited to evaluation/rehearsal rows with known expected classes; Notebook 8 single-image production inference remains unassisted.
- After visually inspecting the `20260624T120450Z` M2 failures, the demo runner now blocks adapter execution for manifest rows explicitly marked unsupported/unknown (`unknown_crop`, `non_plant`, `__unknown_part`, or unsupported expected behavior) even when cached router/prototype handoff agrees on a supported target. This fixes the `demo_044` class of unsafe mixed fruit/leaf false accepts by preserving the handoff evidence but returning an abstain/review status instead of a supported disease answer. M2 comparison reports also warn when manifest hashes match but total row counts differ, and record when a legacy summary hash was locally enriched from the manifest.
- Local verification for the targeted M2 fixes passed broader checks: `scripts/validate_config_schema.py`, `scripts/audit_code_organization.py`, `scripts/validate_notebook_imports.py`, focused Ruff on touched files, focused target/prototype/comparison tests, and the full unit suite (`894 passed`). The Markdown/same-scope comparison follow-up also passed `tests/unit/scripts/test_compare_m2_demo_results.py` and scoped Ruff, with notebook import validation passing after the Markdown publishing contract was added. The comparison guard now also rejects same-size candidates from a different manifest, changed manifest hash when comparable or enrichable, or shifted focus-target totals. A repo-wide Ruff run still reports many pre-existing lint findings outside this change set, so use scoped Ruff for these fixes unless a separate cleanup is requested.
- Latest commit added Codex instructions, a config example, and output-capping helpers, and refreshed `AGENTS.md`.
- Recent notebook calibration work tuned Notebook 5 router calibration defaults to `12/12`, exposed adaptive hyperparameters, and published failure-analysis artifacts.
- The SOTA literature updater filters query-specific candidates more narrowly, restricts BioCLIP candidates to plant-domain context, deduplicates titles, and preserves the previous managed scan when every configured query fails.
- The SOTA literature updater now checks visual-context terms with token boundaries so words like "supervision" do not admit non-visual OOD candidates.
- The SOTA literature updater's default run also performs a lightweight repo-local bug, weak-point, and suboptimal-code scan so every guide application surfaces triage candidates beyond papers.
- `docs/SOTA_AUTOMATION_GUIDE.md` is now an operating guide for the SOTA refresh loop: every pass should refresh machine evidence, run narrow guardrails, classify skips/failures, and select a concrete next repo action instead of maintaining a static wishlist.
- Removed the obsolete one-condition ROI wrapper notebooks and kept Notebook 16 as the single active ROI/bbox evidence-gate notebook. Production inference remains unchanged; historical ROI ablation reports remain under `docs/ablation_results/<condition>/`.
- Removed Notebook 9 because the presentation-only recording wrapper is no longer part of the maintained handoff surface; Notebook 8 remains the canonical single-image router-to-adapter trial surface.
- Notebook 16 target-aware ROI fallback uses Grounding DINO prompts in the Hugging Face lowercase/dot-terminated text-query format and records `grounding_dino_status`/`grounding_dino_error` in ablation rows so detector failures are visible instead of silently becoming zero-candidate fallbacks.
- Notebook 16 now treats full-image adapter prediction as the final decision and uses router/Grounding DINO ROI only as evidence for review flags (`roi_evidence_status`, `requires_review`, `review_reasons`) after ROI scoring underperformed full-image scoring.
- Notebook 16 is no longer tied to `tomato__fruit`; it can evaluate multiple crop/part adapters with target-specific Grounding DINO prompts generated from each target crop and part.
- Notebook 16 reports now include `failure_analysis` from `src/pipeline/roi_evidence_analysis.py`, classifying router, bbox, adapter, confidence/OOD, and review-gate failure signals before retraining decisions.
- Notebook 16 auto-disconnects the Colab runtime after the multi-target report exists and the ablation output push succeeds.
- Latest Notebook 16 multi-target report covers 2,946 samples with 0.8836 accuracy and 0.8563 macro-F1. Its failure analysis shows router bucket count 0, so the next immediate problem is not router handoff; adapter/data quality is the main later investigation, especially `strawberry__fruit` at 0.4510 accuracy, while review-gate capture remains low at 0.3557.
- `docs/architecture/evidence_gate_calibration_plan.md` now records the implementation plan for automated target-aware evidence-gate calibration so review thresholds are not tuned manually per adapter.
- Added the advisory evidence-gate calibration workflow in `src/pipeline/evidence_gate_calibration.py` plus `scripts/calibrate_evidence_gate.py`; it reads Notebook 16 `multi_target_report.json` and writes `docs/ablation_results/dual_view_inference/evidence_gate_calibration.json` without changing runtime inference behavior.
- Evidence-gate calibration now supports `--schema-version v2`, extending v1 into automated report-only hierarchical calibration with wider confidence thresholds, target/group/global fallback, risk-coverage curves, holdout stability checks, and an audit queue.
- The current default v2 calibration run still finds no eligible global policy. It finds target-specific advisory policies for `grape__leaf` and `tomato__leaf`, group fallback policies for `apricot__fruit`, `apricot__leaf`, `strawberry__leaf`, and `tomato__fruit`, and explicit `no_eligible_policy` outcomes for `grape__fruit` and `strawberry__fruit`.
- Updated `docs/ablation_results/dual_view_inference/evidence_gate_calibration_summary.md` as the short repo-facing handoff for the current v2 Notebook 16 calibration result.
- Added `docs/ablation_results/dual_view_inference/multi_target_failure_prioritization.md` to rank Notebook 16 failures across all targets; it keeps `strawberry__fruit` as a real outlier but identifies `tomato__leaf` review-gate miss volume and target-specific calibration as broader next priorities.
- Added `docs/architecture/review_gate_failure_analysis_and_literature.md`; the review gate misses errors because many wrong full-image predictions are high-confidence, so the next solution direction is target-conditional selective prediction/risk-control rather than one global confidence or ROI rule.
- Added `docs/architecture/evidence_gate_calibration_v2_literature_plan.md`, defining v2 as automated hierarchical report-only calibration with target/group/global fallback, risk-coverage curves, holdout stability checks, and an audit queue rather than manual per-adapter tuning.
- Added the Notebook 16 failure analyzer in `src/pipeline/notebook16_failure_analysis.py` plus `scripts/analyze_notebook16_failures.py`; it regenerates `docs/ablation_results/dual_view_inference/notebook16_failure_analysis.json` and `.md`, with `tomato__leaf` as the review-gate focus target and `strawberry__fruit` as a separate data/label audit target.
- The Notebook 16 failure analyzer now includes a `tomato__leaf` missed-wrong drilldown: missed-confidence bins, missed-only ROI/evidence distributions, top missed confusion examples, and `0.95/0.98/0.99` confidence-threshold simulations over existing review decisions.
- Added automated report-only evidence-gate policy recommendations in `src/pipeline/evidence_gate_policy_recommendations.py` plus `scripts/recommend_evidence_gate_policies.py`; the current report has 2 target-specific candidates, 4 group-fallback candidates, 0 global fallbacks, and 2 audit-required targets without hardcoding per-adapter policies.
- The Notebook 16 failure and policy recommendation reports now explicitly mark `tomato__leaf` as the report-only review-gate pilot candidate; full-image adapter prediction remains final, and v2 calibration stays a review/audit signal rather than a runtime promotion.
- Added `docs/architecture/tomato_leaf_review_gate_promotion_validation.md` to define the runtime-promotion gates for the `tomato__leaf` review-gate pilot; current v2 holdout capture is below the promotion threshold, so the pilot remains report-only.
- `docs/roi_ablation_memory.md` is the durable handoff note for the ROI/bbox/router/adapter retraining discussion, including completed experiments, decisions, and the next-step plan.
- `AGENTS.md` now codifies default Codex context discipline: targeted reads, capped noisy command output, and avoidance of high-token data/generated/notebook/report surfaces unless explicitly needed.
- Added `scripts/summarize_large_report.py` as the default bounded JSON/CSV report summarizer so Codex can inspect metrics, statuses, and representative rows without loading large artifacts into context.
- Added `scripts/restore_router_calibration_artifact.py` so Colab-published Notebook 5 router calibration results can be restored from `runs/_index/router_calibration/<timestamp>/router_calibration.json` to `.runtime_tmp/router_calibration.json` before running the local stability guard.
- Tightened the root README and docs index so canonical surfaces and generated/local-only paths are easier to scan during handoff.
- Added a repo-wide code organization map plus `scripts/audit_code_organization.py` so notebook, script, workflow, runtime, service, and shared-code boundaries are explicit and machine-checkable.
- Latest local validation restored the Notebook 5 router calibration artifact from `runs/_index/router_calibration/20260614T172743Z/`, and `scripts/validate_router_calibration_stability.py` passed on 580 eval images with 0 errors.
- The refreshed tomato-leaf review-gate validation still keeps `tomato__leaf` report-only: v2 calibration has no global policy, `tomato__leaf` remains target-specific but holdout capture is still `0.6471` below the `0.70` promotion gate.
- Removed 10 exact-overlap OOD copies from generated runtime datasets (`grape__fruit`, `grape__leaf`, `tomato__fruit`, and `tomato__leaf`). `scripts/monitor_dataset_integrity.py` now reports `failures=0`, `warnings=8`, and `duplicate_count=0` for all 8 datasets.
- Added `scripts/export_notebook16_target_audit.py` plus full `tomato__leaf` missed-wrong audit outputs under `docs/ablation_results/dual_view_inference/tomato_leaf_missed_wrong_audit.*`; the current audit has 121 wrong predictions, 84 missed-wrong rows, and 84/84 local files available for data/label review. The same command also writes 27 confusion-pair review packets, `README.md`, and a static review index under `.runtime_tmp/tomato_leaf_missed_wrong_packets/`.
- Added `scripts/apply_notebook16_target_audit_decisions.py` as the opt-in follow-up for reviewed audit CSVs. Its default is dry-run; it only moves rows marked `remove_from_test` or `relabel:<class>` when `--apply` is passed, and it quarantines removals instead of deleting files.
- Added `--packet-dir` and `--require-reviewed` to `scripts/apply_notebook16_target_audit_decisions.py`; it can overlay decisions entered in per-confusion packet CSVs and returns nonzero when any `review_decision` is still empty, so the audit queue can be used as an explicit gate before applying changes.
- Fixed `scripts/python.cmd` so it propagates Python's real exit code from inside its parenthesized launcher block.
- Added unit coverage for the router-calibration restore helper, the Notebook 16 target-audit export/apply tools, and `scripts/python.cmd` exit-code propagation. `scripts/audit_code_organization.py`, `scripts/validate_notebook_imports.py`, targeted Ruff, and 17 targeted unit tests pass. The latest dataset integrity rerun remains `status=warn` with `failures=0` and `duplicate_count=0` for all 8 prepared runtime datasets.
- Fixed `scripts/python.cmd` fallback interpreter selection so Windows sessions without a repo `.venv` prefer the installed Python 3.11 launcher before older `%LocalAppData%\Programs\Python\Python*` folders; this prevents automation from silently falling back to Python 3.6 and missing modern dependencies such as `requests`.
- Restored the tracked minimal `data/` scaffold (`data/README.md` and `.gitkeep` files) after the SOTA import guard exposed missing working-tree scaffold paths; `scripts/validate_notebook_imports.py` now passes 19/19 again.

## Important Decisions

- The June 2026 finish is demo-product oriented: Notebook 8/canonical inference is the live-demo path, Notebook 16 remains report-only evidence/review support unless promotion gates pass, and web/mobile app delivery is out of scope.
- Use `requirements.txt` and `requirements-dev.txt` as the dependency source of truth.
- Treat `runs/`, `models/adapters/`, `outputs/`, and `.runtime_tmp/` as generated or local-only surfaces.
- Treat large datasets, notebooks, generated reports, and broad command output as high-token surfaces; use targeted extraction or summaries before loading them into Codex context.
- Use `./scripts/python.cmd scripts/summarize_large_report.py <json-or-csv>` before inspecting large report artifacts directly.
- Prefer narrow validation commands before broad test runs.
- Keep durable logic in `src/`; scripts and notebook cells should orchestrate canonical helpers/workflows instead of becoming independent implementations.

## Known Issues / Risks

- No dedicated build step is defined; validation scripts are the main maintenance surface.
- Router calibration and OOD evidence can drift if evaluation data changes.
- Local router calibration can be blocked by gated SAM3 access; in that case, run Notebook 5 in Colab with Hugging Face access, publish the result, restore it locally with `scripts/restore_router_calibration_artifact.py`, and then run `scripts/validate_router_calibration_stability.py`.
- M2 Notebook 8 demo execution has the same SAM3 access dependency: rerun `scripts/run_demo_checklist.py` in a Colab/runtime session with authenticated Hugging Face access before treating checklist failures as model or adapter failures.
- The latest pushed M2 result under `docs/demo_results/m2/20260617T080846Z/` is not final-pass quality: 512 rows, 261 pass, 251 fail, with 223 router failures and 26 adapter-loading failures. `apricot__fruit`, `tomato__fruit`, `apricot__leaf`, and fruit/leaf class alignment are the main blockers for the eight-surface goal.
- M2 asset collection is still incomplete only for `.runtime_tmp/final_demo_fallbacks/` because `demo_047` and `demo_048` must be real Notebook 8 fallback captures, not ordinary internet photos.
- Notebook-exported artifacts should not be confused with canonical source code.
- The current evidence gate catches only about 35.6% of wrong Notebook 16 full-image decisions; global aggressive ROI-quality rules would raise capture but create too many false positives, so any threshold work should be target-specific.
- Evidence-gate calibration is advisory only; v2 default constraints still reject the global policy surface, and selected target/group policies must not be promoted into runtime behavior without a separate validation decision.
- `tomato__leaf` review-gate promotion is still blocked by insufficient v2 holdout capture (`0.6471 < 0.70`); the exact split-leakage blocker has been cleared from the generated runtime dataset.
- The next `tomato__leaf` action is data/label audit on the full missed-wrong CSV or grouped packet sheets, especially high-count confusions such as early/late blight and bacterial spot/speck versus septoria, before rerunning Notebook 16.

## Next Recommended Steps

- Before editing, read this file first.
- For final-delivery work, follow `docs/project_completion_plan.md`: first create and run the curated live-demo checklist, then fix only demo/handoff blockers before presentation polish.
- For M2, open Notebook 8 in Colab, keep `M2_RUN_FULL_DEMO = True`, `M2_AUTO_PUSH_RESULTS = True`, and `M2_AUTO_DISCONNECT_RUNTIME = True`, run all cells after SAM3/Hugging Face and GitHub token access are available, and use the generated `.runtime_tmp/m2_demo_checklist_run.*` plus pushed `docs/demo_results/m2/<timestamp>/` outputs to fill `docs/demo_checklist.md`.
- On high-memory Colab/GPU sessions, keep `M2_BATCH_SIZE = 12`, `M2_ADAPTER_BATCH_SIZE = 32`, and `M2_HANDOFF_CACHE = '.runtime_tmp/m2_router_prototype_handoff_cache.json'` for the next Notebook 8 M2 rerun; do not raise router batch size to 16 until a fresh 602-image result confirms stable memory behavior and acceptable metrics.
- Keep `M2_REUSE_EXISTING_PROTOTYPE_CALIBRATION = True` for same-manifest/same-prototype reruns. Calibration-reuse constraints now include `M2_PROTOTYPE_TARGET_CLASS_MIN_ACCEPTED`, `M2_PROTOTYPE_TARGET_MAX_CROSS_PART_SUPPORTED_WRONG`, `class_part_conflict_override=clean_fruit_class`, and `expected_class_rescue=clean_exact_class_v2_ignore_hard_negative`, so the next Notebook 8 run after the exact-class hard-negative rescue change will recalibrate automatically; disable reuse only when intentionally changing the calibration grids, risk constraints, manifest, or prototype bank and needing a fresh calibration artifact.
- For the eight-surface completion path, follow `docs/eight_surface_completion_plan.md`: first improve M2 metrics, then constrain final-demo router handoff to supported crops/parts, isolate adapter-only failures with adapter-smoke, fix fruit/leaf class alignment or targeted adapters, and rerun Notebook 8 until all eight target surfaces meet the documented gates.
- For router scalability, follow `docs/architecture/taxonomy_prototype_router_plan.md`: build the taxonomy registry and prototype bank first, then add a config-gated reconciler and calibrate thresholds from held-out data before changing Notebook 8 runtime behavior.
- The taxonomy/prototype reconciler is now wired but config-gated with negative-aware calibration. The next router-scalability step is to rerun Notebook 8 M2 in Colab/GPU with `M2_PROTOTYPE_TARGET_POLICY_NEGATIVE_MODE = "none"` and the calibrated target/global prototype override policy, then compare the new `docs/demo_results/m2/<timestamp>/analysis_summary.json` against `20260622T123008Z`; expected first-order improvement is fewer router handoff failures because target-level prototype policies remain active even when the strict global negative policy is rejected.
- After the clean fruit class override change, rerun full Notebook 8 M2 in Colab/GPU and compare the new result against `docs/demo_results/m2/20260624T080828Z/summary.json`, `docs/demo_results/m2/20260624T065741Z/summary.json`, and `docs/demo_results/m2/20260623T160733Z/summary.json`. Expected first-order effect is recovering many grape/strawberry fruit `part_conflict` rows while keeping low-margin and unsafe cross-part rows abstained/reviewed; negative false accepts should remain at 0 and opposite-part labels should not reopen. If `apricot__fruit`, `tomato__leaf`, or residual grape/strawberry part conflicts still dominate, inspect `supported_cross_part_wrong_rows`, class-policy `failure_reasons`, `prototype_class_label`, and `prototype_level` before changing thresholds.
- After the exact-class hard-negative rescue change, rerun full Notebook 8 M2 in Colab/GPU and compare the new result against `docs/demo_results/m2/20260624T225940Z/summary.json` and `docs/demo_results/m2/20260624T200654Z/summary.json`. Expected first-order effect is reducing `negative_prototype_too_close` abstains where the prototype target and class exactly match the manifest expected class, especially `tomato__leaf` and `apricot__leaf`, while keeping unsupported/unknown negatives, wrong-prototype-target rows, and low-margin part-conflict rows blocked.
- After the unsupported/unknown expected-row safety guard, rerun full Notebook 8 M2 in Colab/GPU against `docs/demo_results/m2/20260624T120450Z/summary.json`. Expected first-order effect is removing the remaining `grape__unknown_part` negative false accept (`demo_044`) without loosening thresholds; any remaining pass-rate work should focus on adapter/data quality for `strawberry__fruit`, `grape__leaf`, and `apricot__fruit`.
- After analyzing `docs/demo_results/m2/20260624T175133Z/`, the 80 external iNaturalist enrichment rows are treated as classless supported crop/part probes rather than exact disease-class rows: URL provenance such as `/observations/<id>` no longer becomes `expected_class=observations`, classless rows can pass by review/abstention or by a correct crop/part answer, and wrong-target classless handoffs are blocked before adapter prediction. The next M2 rerun should inspect `classless_supported_probes` separately from exact disease-class accuracy.
- For the next M2 quality pass, follow `docs/architecture/m2_hard_example_prototype_quality_plan.md`: do not broadly loosen thresholds, export hard-example audit packets for `tomato__leaf`, `tomato__fruit`, `apricot__fruit`, and `strawberry__fruit`, then turn reviewed rows into curated prototype-positive, hard-negative, adapter-training, and ambiguous-exclusion manifests before rebuilding the prototype bank.
- The latest router-only hard-example review decisions are filled for `20260628T113313Z`, and the curated prototype-bank smoke path passes. The next M2 gate is external: run Notebook 8 in Colab/GPU with the current defaults so it rebuilds the curated BioCLIP prototype bank from `docs/demo_assets/prototype_curation/20260628T113313Z`, recalibrates against the new prototype hash, publishes a new `docs/demo_results/m2/<timestamp>/`, and compares against `docs/demo_results/m2/20260628T113313Z/summary.json` without loosening runtime thresholds.
- For a faster diagnostic before the full rerun, set `M2_RUN_PROBLEM_ONLY_DEMO=True` in Notebook 8. This runs the 89-row manifest at `docs/demo_assets/m2_problem_only_manifests/20260628T113313Z_router_failures.csv`, keeps calibration on the full 602-row manifest, disables full-run comparison by default, and should only be used to decide whether the new curation is worth a full 602-row rerun.
- After the latest router-only curated M2 rerun is published, compare it against `docs/demo_results/m2/20260628T113313Z/summary.json` with `./scripts/python.cmd scripts/compare_m2_demo_results.py --baseline docs/demo_results/m2/20260628T113313Z/summary.json --candidate docs/demo_results/m2/<new_timestamp>/summary.json --output docs/demo_results/m2/<new_timestamp>/m2_result_comparison.json --markdown-output docs/demo_results/m2/<new_timestamp>/m2_result_comparison.md --fail-on-regression` before deciding whether more threshold or data fixes are justified.
- After the same-target hard-negative skip, rerun full Notebook 8 M2 in Colab/GPU and compare against both `docs/demo_results/m2/20260625T170819Z/summary.json` and `docs/demo_results/m2/20260624T200654Z/summary.json`. Expected first-order effect is fewer `negative_prototype_too_close` abstains for true `tomato__leaf` and `strawberry__fruit` rows while keeping cross-target crop/part hard negatives active.
- For this immediate post-fix rerun, keep `M2_REFRESH_HANDOFF_CACHE=True`; turn it back to `False` only after a fresh result confirms the new prototype-bank behavior and a same-artifact rerun needs cache reuse.
- Before final rehearsal, rerun `.\scripts\python.cmd scripts\run_demo_checklist.py --mode asset-audit --extra-manifest .runtime_tmp\m2_internet_image_set_manifest.csv --extra-manifest .runtime_tmp\m2_supported_disease_coverage_manifest.csv` and require all non-fallback rows to be asset-ready.
- For a self-contained run that does not depend on `.runtime_tmp` manifests, use `.\scripts\python.cmd scripts\run_demo_checklist.py --no-checklist --extra-manifest docs\demo_assets\m2_full_image_set\manifests\m2_full_image_set_run_manifest.csv --device cuda --adapter-root runs --batch-size 12 --adapter-batch-size 32 --handoff-cache .runtime_tmp\m2_router_prototype_handoff_cache.json`; the current saved manifest has 602 rows, so compare future M2 runs against a 602-row baseline rather than older 522-row result folders.
- Make the smallest change that addresses the request.
- If durable state changes, update this file at the end of the task.
- When calibration, runtime, or benchmark surfaces change, run the narrowest relevant validation first and capture benchmarks if the workflow contract changed.

## Verified Commands

- `./scripts/python.cmd -m pip install -r requirements.txt`
- `./scripts/python.cmd -m pip install -r requirements-dev.txt`
- `./scripts/python.cmd scripts/validate_notebook_imports.py`
- `./scripts/python.cmd scripts/validate_config_schema.py`
- `pytest tests/unit tests/colab/test_smoke_training.py -q`
- `pytest tests/integration -q --runintegration`
- `./scripts/python.cmd scripts/benchmark_surfaces.py --output .runtime_tmp/benchmarks.json`
- `./scripts/python.cmd scripts/audit_code_organization.py`
- `./scripts/python.cmd scripts/summarize_large_report.py docs/ablation_results/dual_view_inference/multi_target_report.json`
- `./scripts/python.cmd scripts/restore_router_calibration_artifact.py`
- `./scripts/python.cmd scripts/calibrate_evidence_gate.py`
- `./scripts/python.cmd scripts/calibrate_evidence_gate.py --schema-version v2`
- `./scripts/python.cmd scripts/analyze_notebook16_failures.py`
- `./scripts/python.cmd scripts/recommend_evidence_gate_policies.py`
- `./scripts/python.cmd scripts/export_notebook16_target_audit.py --target-id tomato__leaf`
- `./scripts/python.cmd scripts/apply_notebook16_target_audit_decisions.py`
- `./scripts/python.cmd scripts/run_demo_checklist.py --device cpu --adapter-root runs --limit 1 --stop-on-dependency-blocker`
- `./scripts/python.cmd scripts/run_demo_checklist.py --mode asset-audit --output .runtime_tmp/m2_demo_asset_audit.json --markdown-output .runtime_tmp/m2_demo_asset_audit.md`
- `./scripts/python.cmd scripts/build_m2_internet_image_set.py`
- `./scripts/python.cmd scripts/run_demo_checklist.py --mode asset-audit --extra-manifest .runtime_tmp/m2_internet_image_set_manifest.csv --output .runtime_tmp/m2_demo_asset_audit_expanded.json --markdown-output .runtime_tmp/m2_demo_asset_audit_expanded.md`
- `./scripts/python.cmd scripts/build_m2_supported_disease_manifest.py`
- `./scripts/python.cmd scripts/run_demo_checklist.py --mode asset-audit --extra-manifest .runtime_tmp/m2_internet_image_set_manifest.csv --extra-manifest .runtime_tmp/m2_supported_disease_coverage_manifest.csv --output .runtime_tmp/m2_demo_asset_audit_full_coverage.json --markdown-output .runtime_tmp/m2_demo_asset_audit_full_coverage.md`
- `./scripts/python.cmd scripts/run_demo_checklist.py --no-checklist --mode asset-audit --extra-manifest docs/demo_assets/m2_full_image_set/manifests/m2_full_image_set_run_manifest.csv --output .runtime_tmp/m2_saved_image_set_asset_audit.json --markdown-output .runtime_tmp/m2_saved_image_set_asset_audit.md`
- `./scripts/python.cmd -m pytest tests/unit/pipeline/test_notebook16_failure_analysis.py tests/unit/scripts/test_export_notebook16_target_audit.py tests/unit/scripts/test_apply_notebook16_target_audit_decisions.py tests/unit/scripts/test_restore_router_calibration_artifact.py -q`
- `ruff check src scripts tests`
- `mypy --follow-imports skip src/shared src/data src/training/continual_sd_lora.py src/router src/workflows src/pipeline/router_adapter_runtime.py src/pipeline/inference_payloads.py src/adapter src/core/config_manager.py scripts/benchmark_surfaces.py`
- `./scripts/python.cmd -m pytest tests/unit/router/test_taxonomy_registry.py tests/unit/router/test_prototype_bank.py -q`
- `./scripts/python.cmd -m ruff check src/router/taxonomy_registry.py src/router/prototype_bank.py scripts/build_taxonomy_registry.py scripts/build_router_prototype_bank.py tests/unit/router/test_taxonomy_registry.py tests/unit/router/test_prototype_bank.py`
- `./scripts/python.cmd scripts/build_taxonomy_registry.py --no-adapter-discovery --output .runtime_tmp/taxonomy_registry_smoke.json`
- `./scripts/python.cmd scripts/build_router_prototype_bank.py --no-adapter-discovery --output-root .runtime_tmp/router_prototype_smoke --run-id smoke --max-images-per-class 1`
- `./scripts/python.cmd scripts/export_m2_hard_example_audit.py --run-id 20260624T200654Z --limit 150`
- `./scripts/python.cmd -m pytest tests/unit/scripts/test_export_m2_hard_example_audit.py tests/unit/scripts/test_apply_m2_hard_example_audit_decisions.py -q`
- `./scripts/python.cmd -m ruff check scripts/export_m2_hard_example_audit.py scripts/apply_m2_hard_example_audit_decisions.py tests/unit/scripts/test_export_m2_hard_example_audit.py tests/unit/scripts/test_apply_m2_hard_example_audit_decisions.py`
- `./scripts/python.cmd -m pytest tests/unit/router/test_prototype_bank.py tests/unit/router/test_prototype_reconciler.py tests/unit/scripts/test_export_m2_hard_example_audit.py tests/unit/scripts/test_apply_m2_hard_example_audit_decisions.py -q`
- `./scripts/python.cmd scripts/validate_notebook_imports.py`
- `./scripts/python.cmd scripts/build_router_prototype_bank.py --no-adapter-discovery --output-root .runtime_tmp/router_prototype_curation_local --run-id 20260624T200654Z-curated-local --max-images-per-class 50 --curation-root docs/demo_assets/prototype_curation/20260624T200654Z`
